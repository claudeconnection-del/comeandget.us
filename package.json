{
  "name": "comeandget-us",
  "version": "1.0.0",
  "private": true,
  "description": "A static cryptographic ARG landing page for comeandget.us",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "validate": "html-validate \"site/**/*.html\"",
    "test": "playwright test",
    "ci": "npm run validate && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "html-validate": "^9.5.3",
    "http-server": "^14.1.1",
    "wrangler": "^3.99.0"
  }
}
